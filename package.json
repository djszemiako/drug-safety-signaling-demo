{
  "name": "drug-safety-signaling",
  "version": "1.0.0",
  "private": true,
  "description": "Viewer for FDA label change safety signals (issue #2995)",
  "module": "src/server.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "sync": "bun run src/sync.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "duckdb-async": "^1.1.3"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
